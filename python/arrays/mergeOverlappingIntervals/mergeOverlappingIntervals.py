def mergeOverlappingIntervals(intervals):
    i = 0
    # mergedArray = []
    while i < len(intervals):
        # checkingArray = []
        # candidates = [interval for interval in intervals if interval[0] < 1]

        break

    return [[]]
